# graph/subgraph

Subgraph definitions for the CloverDX sandbox. Contains `.sgrf` files — reusable graph fragments that encapsulate common processing patterns (such as error handling) and can be embedded in multiple parent graphs.
