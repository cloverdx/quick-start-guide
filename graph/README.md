# graph

Transformation graphs for the CloverDX sandbox. Contains `.grf` files that define data transformation workflows — the core building blocks of CloverDX data processing. Each graph specifies components, connections, and transformation logic.
