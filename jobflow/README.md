# jobflow

Jobflow orchestration definitions for the CloverDX sandbox. Contains `.jbf` files that orchestrate the execution of multiple graphs and tasks in a defined sequence, handling dependencies, error recovery, and conditional logic.
