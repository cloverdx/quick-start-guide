# data-service

REST API data service definitions for the CloverDX sandbox. Contains `.rjob` files that expose CloverDX graphs as REST endpoints, enabling external systems to trigger data processing via HTTP requests.
