# meta

Metadata definitions for the CloverDX sandbox. Contains `.fmt` files that define record structures (field names, data types, delimiters) describing the format of data flowing through transformation graphs.
