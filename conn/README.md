# conn

Database connection configurations for the CloverDX sandbox. Contains `.cfg` files that define connection parameters (JDBC URL, credentials, driver) for databases used by graphs and jobflows.
