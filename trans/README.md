# trans

Transformations directory for the CloverDX sandbox. Stores Java and CTL (CloverDX Transformation Language) source files that define custom transformation logic used by components in graphs.
